# 数据目录（已停用）

> 自 2026-08-26 起，代码不再读写本目录。

所有用户数据统一存放在用户主目录下：

| 数据 | 实际位置 |
|------|----------|
| 档案 | `~/.career-kit/` |
| 知识库 | `~/.career-kit/knowledge/` |
| 缓存 | `~/.career-kit/cache/` |

路径常量统一定义在 `src/paths.py`，可用环境变量 `CAREER_KIT_DATA_DIR` 覆盖根目录。

本目录保留作为测试样例数据的存放点；历史抓取数据已迁移至用户主目录。
